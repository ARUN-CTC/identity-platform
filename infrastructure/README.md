# Infrastructure

Empty in Phase 1 on purpose. This project's only Phase 1 infrastructure is `docker-compose.yml` at the repository root (the local Postgres instance).

Reserved for Phase 2+: Kubernetes manifests, Terraform/IaC, CI/CD pipeline definitions — none exist yet, and Phase 1's explicit scope excludes production HA/multi-region/Kubernetes deployment (see `docs/PHASE_1.md`).
